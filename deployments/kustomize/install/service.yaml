kind: Service
apiVersion: v1
metadata:
  name: app-ambulance-webapi
spec:
  selector:
    pod: app-ambulance-webapi-label
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: webapi-port